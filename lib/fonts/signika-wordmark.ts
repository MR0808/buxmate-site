import { Signika } from "next/font/google";

/** Wordmark only — use on Logo, not in root layout. */
export const signikaWordmark = Signika({
  subsets: ["latin"],
  weight: ["600"],
  display: "swap",
});
